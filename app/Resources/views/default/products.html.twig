{% extends 'base.html.twig' %}


{% block body %}
        {% for product in products %}

            <div class="product">
                {{ product.name }} <br>
                {{ product.price|number_format(2, '.', ',') }} PLN
            </div>

        {% endfor %}
{% endblock %}